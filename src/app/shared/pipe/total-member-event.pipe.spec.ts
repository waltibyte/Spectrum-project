import { TotalMemberEventPipe } from './total-member-event.pipe';

describe('TotalMemberEventPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalMemberEventPipe();
    expect(pipe).toBeTruthy();
  });
});
